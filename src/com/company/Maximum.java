package com.company;

public class Maximum {
    int max(int x, int y) {
        return x >= y ? x : y;
    }

    int homeWork(boolean bl, double d) {
        return bl ? 0 : (int) d;
    }

    boolean isFirstMax(int x, int y) {

        return x > y;
    }
}
